using Int64;

public class Expression {
    static long x;
    static long f(long y) {
        long z;
        z = x + y;
        return 0;
    }
    public static void Main() {
        int r = (int) Utils.Pow(2, 10);
    }
}